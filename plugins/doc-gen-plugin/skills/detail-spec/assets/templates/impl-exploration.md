# {機能/プロジェクト名} - 実装前コードベース探索

> **ステータス**: 探索中 | 探索完了
> **目的**: impl-detail-spec を書く前に、コードベースを探索して技術的事実を収集する
> **出力先**: この探索結果をもとに `impl-detail-spec` を作成する

## 1. 探索の目的

### 1.1 何を実装したいか

{実装したい機能・変更の概要。1〜3文}

### 1.2 探索で明らかにしたいこと

<!-- impl-detail-spec を正確に書くために、コードベースから収集すべき情報 -->

- [ ] 技術スタックの実態（バージョン、ビルドツール、テストツール）
- [ ] 関連する既存コードの場所と構造
- [ ] 既存のパターン・規約（命名、ディレクトリ構成、エラーハンドリング）
- [ ] 変更が必要なファイルと影響範囲
- [ ] 既存の型・インターフェース・データ構造
- [ ] テストの構造と実行方法
- {追加の探索観点}

## 2. 技術スタック確認

<!-- package.json, go.mod, Cargo.toml, pyproject.toml 等から実際の値を読み取って記録する -->

| 項目 | 値 | 確認元 |
|:-----|:---|:-------|
| 言語/バージョン | {実際の値} | {ファイルパス} |
| フレームワーク | {実際の値} | {ファイルパス} |
| データベース | {実際の値} | {ファイルパス} |
| テストツール | {実際の値} | {ファイルパス} |
| パッケージマネージャー | {実際の値} | {ファイルパス} |
| ビルドコマンド | `{実際のコマンド}` | {ファイルパス} |
| テストコマンド | `{実際のコマンド}` | {ファイルパス} |
| リントコマンド | `{実際のコマンド}` | {ファイルパス} |
| 型チェックコマンド | `{実際のコマンド}` | {ファイルパス} |

## 3. ディレクトリ構成の把握

<!-- プロジェクト全体の構成を記録し、今回の変更に関連する領域を特定する -->

### 3.1 プロジェクト全体構成

```
{実際のディレクトリツリー}
```

### 3.2 関連ディレクトリの詳細

<!-- 変更対象に近い領域を詳細に記録する -->

```
{関連領域の詳細なディレクトリツリー}
```

### 3.3 ディレクトリ構成の規則

<!-- 既存のファイル配置から読み取れるルール -->

| 規則 | 例 |
|:-----|:---|
| {例: feature ごとにディレクトリを切る} | {例: `src/features/auth/`, `src/features/user/`} |
| {例: テストは同一ディレクトリに配置} | {例: `handler.ts` → `handler.test.ts`} |

## 4. 既存コード分析

### 4.1 関連ファイル一覧

<!-- 今回の実装に関連する既存ファイルを探索して記録する -->

| ファイル | 責務 | 関連度 | 変更要否 |
|:---------|:-----|:-------|:---------|
| `{パス}` | {このファイルが何をしているか} | 高/中/低 | 変更/参照のみ |

### 4.2 既存の型・インターフェース

<!-- 今回の実装で使用・拡張する既存の型定義 -->

```typescript
// {ファイルパス} より
{既存の型定義をそのまま記録}
```

### 4.3 既存の関数・メソッド

<!-- 呼び出す・拡張する既存の関数 -->

| 関数 | ファイル | シグネチャ | 用途 |
|:-----|:---------|:----------|:-----|
| `{関数名}` | `{パス}` | `{シグネチャ}` | {今回の実装での利用意図} |

### 4.4 外部依存（ライブラリ・API）

<!-- 今回の実装で使用する既存の外部依存 -->

| 依存 | バージョン | 用途 |
|:-----|:----------|:-----|
| `{パッケージ名}` | {バージョン} | {今回の実装での利用意図} |

## 5. パターン・規約の抽出

<!-- 既存コードから実際のパターンを読み取って記録する。新規コードはこれに従う -->

### 5.1 命名規約

| 対象 | 規約 | 既存の実例 |
|:-----|:-----|:----------|
| ファイル名 | {例: kebab-case} | {例: `user-service.ts`} |
| 関数名 | {例: camelCase} | {例: `getUserById`} |
| 型名 | {例: PascalCase} | {例: `UserResponse`} |
| テストファイル | {例: `*.test.ts`} | {例: `user-service.test.ts`} |

### 5.2 アーキテクチャパターン

<!-- 既存コードで採用されているパターンを具体的なコード例とともに記録 -->

| パターン | 適用箇所 | コード例の場所 |
|:---------|:---------|:-------------|
| {例: Repository パターン} | {例: データアクセス層} | `{例: src/repositories/user-repository.ts}` |

### 5.3 エラーハンドリングパターン

<!-- 既存コードのエラー処理方法を記録する -->

```typescript
// {ファイルパス} より - 既存のエラーハンドリング例
{実際のコードスニペット}
```

### 5.4 ログ・監視パターン

<!-- 既存のログ出力パターンがあれば記録 -->

```typescript
// {ファイルパス} より
{実際のコードスニペット}
```

## 6. データ構造の調査

### 6.1 既存のDBスキーマ

<!-- マイグレーションファイル、スキーマ定義、ORM モデル等から読み取る -->

| テーブル/コレクション | 主要カラム | 関連 | 確認元 |
|:---------------------|:----------|:-----|:-------|
| `{テーブル名}` | {主要カラム} | {FK等} | `{ファイルパス}` |

### 6.2 既存の状態管理

<!-- フロントエンドの状態管理、キャッシュ構造等 -->

| 状態/ストア | 管理方法 | 確認元 |
|:-----------|:---------|:-------|
| `{状態名}` | {例: Redux slice / React Context / Zustand} | `{ファイルパス}` |

## 7. 処理フロー分析

### 7.1 類似機能の処理フロー

<!-- 今回の実装に似た既存機能がある場合、その処理フローを記録する -->

**{類似機能名}** (`{ファイルパス}`):

```mermaid
flowchart TD
    A[{ステップ1}] --> B[{ステップ2}]
    B --> C[{ステップ3}]
```

### 7.2 エントリポイントと呼び出しチェーン

<!-- リクエストの入口から処理の流れを追跡する -->

```
{エントリポイント}
  → {呼び出し先1} ({ファイルパス}:{行番号})
    → {呼び出し先2} ({ファイルパス}:{行番号})
```

## 8. テスト構造の調査

### 8.1 テストディレクトリ構成

```
{テスト関連のディレクトリツリー}
```

### 8.2 テストパターン

<!-- 既存テストの書き方を記録する -->

```typescript
// {ファイルパス} より - 既存テストのパターン例
{実際のテストコードスニペット}
```

### 8.3 テスト実行方法

| コマンド | 用途 |
|:---------|:-----|
| `{コマンド}` | 全テスト実行 |
| `{コマンド}` | 特定ファイルのテスト |
| `{コマンド}` | カバレッジ付き実行 |

## 9. 変更影響範囲

### 9.1 変更が必要なファイル

| ファイル | 変更内容 | 理由 |
|:---------|:---------|:-----|
| `{パス}` | {何を変更するか} | {なぜ変更が必要か} |

### 9.2 変更してはいけないファイル

<!-- 変更すると他機能に影響が出る、またはプロジェクト規約で禁止されている領域 -->

| ファイル/ディレクトリ | 理由 |
|:---------------------|:-----|
| `{パス}` | {触ってはいけない理由} |

### 9.3 影響を受ける既存機能

| 機能 | 影響内容 | 対策 |
|:-----|:---------|:-----|
| {機能名} | {どう影響するか} | {影響を最小化する方法} |

## 10. 発見事項・懸念点

### 10.1 重要な発見

<!-- 探索で判明した、impl-detail-spec に反映すべき重要な事実 -->

- {発見事項}

### 10.2 懸念点・リスク

<!-- 実装にあたって注意すべき点 -->

| 懸念点 | 影響 | 対策案 |
|:-------|:-----|:-------|
| {懸念点} | {影響} | {対策案} |

### 10.3 未解決の疑問

<!-- 探索だけでは判明しなかった、ユーザーへの確認が必要な事項 -->

- [ ] {確認が必要な事項}

## 11. impl-detail-spec 入力サマリー

<!-- この探索結果から impl-detail-spec の各セクションに渡す情報を整理する -->

| impl-detail-spec セクション | この探索で判明した入力 |
|:---------------------------|:--------------------|
| §2 技術コンテキスト | セクション2 の結果をそのまま使用 |
| §3 変更禁止事項 | セクション9.2 の結果を使用 |
| §4 ファイル構成 | セクション3 + セクション9.1 をもとに設計 |
| §5 インターフェース定義 | セクション4.2, 4.3 の既存型を拡張して設計 |
| §6 データ構造 | セクション6 の既存スキーマをもとに設計 |
| §7 処理フロー | セクション7 の既存フローを参考に設計 |
| §8 エラーハンドリング | セクション5.3 のパターンに従って設計 |
| §9 実装パターン・規約 | セクション5 の結果をそのまま使用 |
| §10 テスト要件 | セクション8 のパターンに従って設計 |
